# Critic Rescue Evaluation

The normal evaluation run completed but did not produce structured output.
Score the architecture files against the sprint contract below.

## Sprint Contract

{contract_json}

## Architecture Files

{files_text}

## Task

This is Round {round_num}. Score **every** criterion in the contract. Apply the
same scoring standards and severity rules from your system prompt.

Return ONLY a `CriticResult` JSON object — no preamble, no explanation, no code fences.
